- Need to prevent creating workers from XNode selection.
- Create a block tree UI for dropping blocks
- Easier creation of channel wire endpoints from blocks
- handle control ref inputs
- Tools menu entries
- If removing Init case, need to do remove init